function strain = PlasticityMain(STRAIN,SIGMA,TimeTotal,istep)


%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
% Returning Map Algorithm 
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%

%5 (paths)
delta_t=TimeTotal/istep/5;

% Strain Rate
eps_rate=[];
for i=1:size(STRAIN)
    i=i+1
    eps_rate(i)=(STRAIN(i)-STRAIN(i-1))/delta_t;
    
    eps_trial=maps_plas(eps_rate,SIGMA)
end





    